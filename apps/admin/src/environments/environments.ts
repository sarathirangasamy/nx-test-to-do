export const environment = {
    port:"http://localhost:3000/api/toDo"
  };


  export const config = {
    headers: {
      'Content-Type': 'application/json',
      // Add any other headers as needed
    },
  };

